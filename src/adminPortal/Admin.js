import React from 'react';
import Header from './Header';
import './Header.scss';

const Admin =()=>{
    return(
        <div>
            {/* Add header component 1. Ribbon image as functional 2. Search as classbased 3. Avtar as class based*/}
            <Header/>
            {/* Add Nav bar Use nav bar as functional but the popup as classbased*/}
            {/*Content body: Image as functional and use of mapping to show the data */}
        </div>   
    );
}
export default Admin;